import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zakah.calculator',
  appName: 'Zakat Calculator',
  webDir: 'www',
  android: {
    backgroundColor: '#0d1117',
  },
};

export default config;
